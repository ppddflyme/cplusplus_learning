#include <iostream>

using std::cout;
using std::cin;
using std::endl;

int main() {
  int a = 0, b = 0;
  cout << "Please input two data:" << endl;
  cin >> a >> b;  
  cout << "a * b = " << (long)a * b << endl;
  return 0;
}
